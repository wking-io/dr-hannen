import '../css/home.css';

// function handleVideoComplete(video) {
//   return function() {
//     if (video.duration - video.currentTime <= 0.75) {
//       const videoParent = document.querySelector('.hero-video');
//       if (videoParent) {
//         videoParent.setAttribute('data-video-state', 'complete');
//       }
//     }
//   };
// }

// const video = document.getElementById('hero-video');
// if (video) {
//   video.addEventListener('timeupdate', handleVideoComplete(video), false);
// }
